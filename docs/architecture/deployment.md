# Deployment architecture

Home Connect Oven Card is deployed as Home Assistant frontend browser runtime. Deployment and release automation validate the repository before mutating runtime state. See the [deployment diagram](diagrams/deployment.md).
