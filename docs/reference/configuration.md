# Configuration

- `type: custom:oven-card` selects the card.
- `device_id` enables Home Assistant device-based entity discovery.
- Explicit entity mappings override discovery for unusual installations.
- Display and programme-name options control optional UI behavior.

Secrets are referenced from protected runtime stores and must never be committed.
