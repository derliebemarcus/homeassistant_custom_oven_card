{
  "name": "homeassistant_custom_oven_card",
  "version": "0.1.2",
  "private": true,
  "type": "module",
  "description": "A standalone Home Assistant card for Home Connect ovens.",
  "scripts": {
    "build": "node scripts/build.mjs",
    "check": "node --check src/homeassistant_custom_oven_card.js && node --check dist/homeassistant_custom_oven_card.js",
    "test": "npm run check && node tests/validate.mjs"
  },
  "engines": {
    "node": ">=24"
  },
  "license": "MIT"
}
